apiVersion: v1
kind: PersistentVolume
metadata:
  name: surcharge-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/surcharge-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: surcharge-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: surcharge-db-deployment
  labels:
    app: surcharge-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: surcharge-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: surcharge-db-deployment
        tier: database
    spec:
      containers:
        - name: surcharge-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: surcharge-db-data
      volumes:
        - name: surcharge-db-data
          persistentVolumeClaim:
            claimName: surcharge-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: surcharge-db-service
  labels:
    app: surcharge-db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: surcharge-db-deployment
    tier: database
  type: LoadBalancer