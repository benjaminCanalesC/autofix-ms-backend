apiVersion: v1
kind: PersistentVolume
metadata:
  name: discount-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/discount-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: discount-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: discount-db-deployment
  labels:
    app: discount-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: discount-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: discount-db-deployment
        tier: database
    spec:
      containers:
        - name: discount-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: discount-db-data
      volumes:
        - name: discount-db-data
          persistentVolumeClaim:
            claimName: discount-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: discount-db-service
  labels:
    app: discount-db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: discount-db-deployment
    tier: database
  type: LoadBalancer